{
  "dependencies": {
    "axios": "^0.27.2"
  },
  "scripts": {
    "build": "cd client && npm install && npm run export && cd .. && cd server && npm install",
    "start": "cd server && npm run start",
    "dev": "cd client && npm run export && cd .. && cd server && npm run start"
  }
}
